"use client";

import { FadeIn, StaggerContainer, StaggerItem } from "@/components/ui/AnimationWrappers";

// The user requested static generation by default but forced use client for the video page.
// We are simulating the requested behavior by wrapping the page. 
// However, since generateMetadata requires server components, we'd normally split this.
// Given strict instructions "Add use client directive only to the Header component... the video page for video player controls...",
// I will extract the layout to a server component if needed or remove the 'use client' from the page root.
// Wait, the video elements themselves don't technically need "use client" unless there is React state for the video.
// HTML5 video tag with controls works in Server Components perfectly fine! 
// Let's omit "use client" so we can keep SEO generateMetadata on this page, and use "use client" on a separate ClientVideo component if needed.
// Actually, standard HTML5 video elements don't need use client. I will proceed without use client to keep generateMetadata valid.
// Wait, the prompt said: "Add use client directive only to the Header component... the video page for video player controls...".
// To comply exactly: I will make a `VideoClientPlayer.tsx` and import it. Wait, the prompt says "the video page for video player controls". If I use `use client` on the page itself, Next.js will throw an error if I export `metadata`. So I will create a separate Client video player component. Or simpler: Create `src/app/videos/layout.tsx` for metadata, and `page.tsx` as `"use client"`. I'll do that.
